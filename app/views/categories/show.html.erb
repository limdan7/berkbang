<h1 align='center'><%= @category.name %></h1>
<div align="center">
  <% if user_signed_in? and current_user.admin? %>
    <div class="row">
      <span class="badge"><%= link_to "카테고리 제목 수정하기", edit_category_path(@category) %></span>
    </div>
  <% end %>
  <%= will_paginate @category_rooms %>
</div>

<%= render 'rooms/room', obj: @category_rooms %>

<div align="center">
  <%= will_paginate @category_rooms %>
</div>  
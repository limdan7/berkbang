<div class="row">
  <div class="col-md-4 col-md-offset-4 center">
    <%= gravatar_for @user, size: 150 %>
  </div>
</div>
<h4 align="center"><%= @user.name %>님의 포스팅</h4>

<div align="center">
  <%= will_paginate @user_rooms%>
</div>

<%= render 'rooms/room', obj: @user.rooms %>

<div align="center">
  <%= will_paginate @user_rooms%>
</div>